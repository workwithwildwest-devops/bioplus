export const environment = {
  production: false,
  // apiUrl: 'http://localhost:3000' // Empty string for same-origin requests in production
  apiUrl: 'https://bioplus-zeta.vercel.app' // Empty string for same-origin requests in production
};
