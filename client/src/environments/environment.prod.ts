export const environment = {
  production: true,
  // apiUrl: 'http://localhost:3000' // Empty string for same-origin requests in production
  apiUrl: 'https://bioplus-seven.vercel.app' // Empty string for same-origin requests in production
};
